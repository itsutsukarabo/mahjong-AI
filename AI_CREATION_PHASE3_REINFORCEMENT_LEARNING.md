# AI作成 フェーズ3: 強化学習フレームワーク実装計画

作成日: 2026-05-27

---

## 目的

フェーズ2で学習したモデル（手牌類推・行動クローン・V関数）を初期値として、
AI同士のセルフプレイによる強化学習を行い、「人間の平均を超える」AIを育成する。

フェーズ2の行動クローンは人間を模倣するだけで、人間の平均が上限になる。
強化学習（RL）では点数状況・着順を最適化する戦略を自律的に学習し、人間を超えることができる。

---

## 作成するAIの種類

2種類のAIを別々に学習し、比較することで「どの目的関数が実力に効くか」を検証する。

### Type A: 局内最大得点AI

```
最適化対象: 1局あたりの和了点数の期待値
ベースライン: 現行ルールベースAI（eval_shoupai）の延長
点棒状況の考慮: しない
典型的な行動: 常に最高点の手を狙う。ラス目でも安手を選ばない
```

現行 `eval_shoupai()` の延長線上にある。フェーズ3の導入・検証用として先に動かす。

### Type B: ゲーム終了逆算AI

```
最適化対象: 対局終了時の着順点（ウマ後ポイント）
点棒状況の考慮: する（ラス回避・トップ争い・着順ボーダーを意識）
典型的な行動: ラス目なら安手でも即和了を選ぶ。トップ目なら守りを固める
```

本命のAI。着順を意識した戦略が学習できる。フェーズ2のV(状態)初期モデルを引き継ぐ。

---

## 対局ループ基盤

### `do_sync()` による高速自己対戦

`majiang-core` の `Game.do_sync()` を使うことで1局がミリ秒単位で完走する。
ブラウザのイベントループを使わず Node.js で同期実行するため、バッチ学習に適している。

```js
const Majiang = require('@kobalab/majiang-core');

// 基本の対戦ループ
function run_games(agents, n_games, rule) {
    const results = [];
    for (let i = 0; i < n_games; i++) {
        const game = new Majiang.Game(
            agents.map(a => a.create_player()),
            (paipu) => results.push(paipu),
            rule
        );
        game.do_sync();
    }
    return results;
}

// paipu から報酬を取り出す
function extract_reward(paipu, player_id) {
    return {
        final_point:  paipu.point[player_id],   // ウマ後ポイント
        final_rank:   paipu.rank[player_id],    // 最終順位 (1-4)
        final_defen:  paipu.defen[player_id],   // 最終持ち点
        log:          paipu.log                 // 全局の牌譜ログ（局単位の収支計算に使う）
    };
}
```

### ゲームデータの記録

```js
// 学習に必要なトランジションデータ
// (状態, アクション, 報酬, 次の状態) のセット
{
    state:      encode_state(player_model, suanpai),  // 状態ベクトル
    action:     encode_action(chosen_action),          // アクションベクトル
    reward:     0,                     // 中間報酬（TD法で計算）
    next_state: encode_state(...),     // 次の状態
    done:       false,                 // 対局終了フラグ
    final_point: null                  // 対局終了時に埋まる
}
```

---

## 観測空間（状態表現）

AIに「見せる情報」の定義。

| カテゴリ | 情報 | 取得先 |
|---|---|---|
| 自手牌 | 各牌の枚数（bingpai） | `player.shoupai._bingpai` |
| 副露内容 | 自分の面子一覧 | `player.shoupai._fulou` |
| 向聴数 | 現在の向聴数 | `Util.xiangting(player.shoupai)` |
| テンパイ牌 | テンパイ牌一覧と残り枚数 | `Util.tingpai()`, `SuanPai._paishu` |
| 残り牌数 | 全牌の残り枚数（推定） | `SuanPai._paishu` |
| 残りツモ数 | 残りツモ回数 | `SuanPai._n_zimo` |
| 他家捨て牌 | 各プレイヤーの捨て牌セット | `SuanPai._dapai` |
| リーチ状態 | 各プレイヤーのリーチ有無 | `SuanPai._lizhi` |
| ドラ情報 | ドラ表示牌一覧 | `player.shan.baopai` |
| 点数状況 | 全員の現在点数 | `player.model.defen` |
| 着順状況 | 現在の暫定着順 | `player.model.defen` から計算 |
| 場況 | 場風・門風・本場・残り局数 | `player.model.zhuangfeng`, `player._menfeng` 等 |
| 手牌推定（フェーズ2） | 他家の手牌確率（周辺確率） | フェーズ2の手牌類推モデル |

---

## 行動空間

AIが選択できるアクションの定義。

| アクション種別 | 生成方法 | 備考 |
|---|---|---|
| 打牌 | `Game.get_dapai(rule, shoupai)` | リーチ宣言は `*` を付加 |
| チー | `Game.get_chi_mianzi(rule, shoupai, p, paishu)` | |
| ポン | `Game.get_peng_mianzi(rule, shoupai, p, paishu)` | |
| カン | `Game.get_gang_mianzi(rule, shoupai, p, paishu, n_gang)` | |
| 和了 | `{ hule: '-' }` | `allow_hule()` が true の場合 |
| パス | `{}` / `callback()` | 和了・副露をしない選択 |
| テンパイ宣言 | `{ daopai: '-' }` | `allow_no_daopai()` が true の場合 |

---

## 報酬設計

### 基本思想: 報酬整形（Reward Shaping）

対局終了まで待たずに中間報酬を与える。

```
報酬_t = V(状態_{t+1}) - V(状態_t)

V(状態) = この局面から対局終了までの着順点の期待値
```

これは TD（時間差分）学習の基本形と同じ。アクションごとに「着順期待値がどれだけ変化したか」を報酬として与える。

### Type A（局内最大得点AI）の報酬

```
中間報酬: eval_shoupai(次状態) - eval_shoupai(現状態)
  ← 現行 eval_shoupai() をそのまま使う

局終了報酬: その局の実際の収支（得点 or 失点）
```

### Type B（ゲーム終了逆算AI）の報酬

```
中間報酬: V_critic(次状態) - V_critic(現状態)
  ← フェーズ2のV(状態)初期モデルから始まり、学習中に更新される

対局終了報酬: paipu.point[player_id]（ウマ後ポイント）
  ← 最も純粋な最終報酬
```

### 報酬遅延と着順の難しさ

「着順を目的とさせると特に難しくなる」という認識は正しい。理由は：
- 打牌の良し悪しが分かるのは最終結果（数十手後）のみ
- 4人ゲームの競争なので、自分が良い手を打っても他家次第で順位が変わる

上記の報酬整形（V関数の差分）はこの問題への直接的な解答で、
「そのアクションが着順期待値を上げたか下げたか」を即時フィードバックできる。

---

## Actor-Critic アーキテクチャ

循環問題（V関数を計算するにはV関数が必要）の解決策。

```
Actor（方策）: 状態 → アクション確率
  - フェーズ2の行動クローンで初期化
  - 学習中にV関数の勾配信号で更新

Critic（価値関数）: 状態 → V(状態)
  - フェーズ2のV(状態)初期モデルで初期化
  - 学習中に実際の対局結果（ベルマン方程式）で更新

更新サイクル:
  1. Actor が行動を選択
  2. 環境が次状態と報酬を返す
  3. Critic が V(現状態) と V(次状態) を計算
  4. TD誤差 = 報酬 + γ × V(次状態) - V(現状態)
  5. Actor をTD誤差で更新（方策勾配）
  6. Critic をTD誤差で更新（最小二乗法）
```

---

## 対戦相手の設計: 過去世代との対戦

ランダムエージェントを対戦相手にすると「ランダムを崩す戦略」に特化したAIができてしまい、
実際の対局には通用しない（分布シフト問題）。

**推奨: Fictitious Self-Play（過去世代との対戦）**

```
世代管理プール: [agent_v1, agent_v2, ..., agent_vN（最新）]

各対局での座席構成（例）:
  席0: agent_vN（学習中の最新AI）
  席1〜3: プールからランダムに選んだ過去世代のAI

一定の対局数ごとに:
  - agent_vN のチェックポイントをプールに追加
  - プールが大きくなりすぎたら古い世代を削除（最新N世代を保持）
```

この方式で「多様な相手との対戦」と「自分自身との一貫した最適化」を両立する。

---

## 評価・ベンチマーク

### AIの強さの測り方

| 評価方法 | 説明 |
|---|---|
| 現行ルールベースAI（majiang-ai v1.1.0）との対戦成績 | ベースラインとの比較 |
| Type A vs Type B の直接対決 | 目的関数の違いが実力に与える影響 |
| 過去世代との Elo レーティング | 学習曲線の可視化 |
| 特定局面での判断比較 | フェーズ1のモーダルで人間と比較 |

### 評価の記録

```
benchmark_results/
  YYYYMMDD_HHMM/
    config.json        ← 対戦設定・使用モデルバージョン
    win_rates.json     ← 1位率・2位率・3位率・4位率
    avg_point.json     ← 平均ポイント
    games.jsonl        ← 全対局ログ（分析モーダルで再生可能）
```

---

## 現行ルールベースAIの定数の再検証

現行AIには経験的に決められた定数が複数ある。フェーズ3のRLで最適化できる対象。

| パラメーター | 場所 | 現在値 | 意味 |
|---|---|---|---|
| `width[0]` | `player.js:9` | `8` | テンパイ時の評価正規化幅 |
| `width[1]` | `player.js:9` | `32` | 1向聴時の評価正規化幅 |
| `width[2]` | `player.js:9` | `64` | 2向聴時の評価正規化幅 |
| 危険度上限1 | `player.js:332` | `13.0` | 無条件回避しきい値 |
| 危険度上限2 | `player.js:333,335` | `8.0` | 条件付き回避しきい値 |
| 安全牌基準 | `player.js:335` | `3.2` | 安全牌の危険度上限 |
| 副露期待値下限（テンパイ） | `player.js:170` | `750` | リーチ者存在時のポン/チー基準 |
| 副露期待値下限（和了） | `player.js:171` | `250` | テンパイ取りの基準 |
| リーチ期待値下限 | `player.js:392` | `350` | リーチ宣言の期待値基準 |
| `paijia` 倍率 | `suanpai.js:201-207` | `2, 4, 8` | 役志向時の牌価値ブースト |

**注意**: `width` を変えると期待値のスケールが変わるため、他のすべての閾値も連動して調整が必要。
定数を個別にチューニングするよりも、フェーズ3のRL全体で「方策として学習させる」方が根本的な解決になる。

---

## 着順期待値の変動データ（点数→着順の変換モデル）

Type Bで「どんなアガリ・振込が着順期待値をどう変動させるか」を計算するには、
「現在の点数分布」から「着順点の期待値」を変換するモデルが必要。

```
score_to_rank_ev(点数状況, 残り局数, 残り本場) → 着順点の期待値
```

これはシミュレーションから導出可能（残り局数×点数ランダムサンプリング）か、
フェーズ2のV(状態)モデルの副産物として得られる。

ここは純粋に「決定論的な計算」に近く（ランダム要素があるが計算可能）、
教師あり学習よりもモンテカルロシミュレーションの方が適切かもしれない。

---

## 実装ステップ

### 対局ループ基盤

1. `do_sync()` を使ったN局バッチ実行スクリプトを作成する
2. 対局結果（点数・着順・牌譜ログ）の記録モジュールを作成する
3. 状態エンコーダー `encode_state()` を実装する
4. 行動エンコーダー `encode_action()` を実装する
5. 対戦相手管理（過去世代プール）を実装する

### Type A（局内最大得点AI）

6. 現行 `eval_shoupai()` をベースにした報酬計算を実装する
7. Actor-Critic の Actor 部分を実装する（行動クローンで初期化）
8. Critic に `eval_shoupai()` を使った報酬整形を組み込む
9. 過去世代との対戦ループを回す
10. 現行ルールベースAIとのベンチマークを実行する

### Type B（ゲーム終了逆算AI）

11. フェーズ2のV(状態)初期モデルをCriticとして読み込む
12. Type B 用の報酬計算（ウマ後ポイントベース + 報酬整形）を実装する
13. Actor-Critic を実装する（行動クローンで初期化）
14. 過去世代との対戦ループを回す
15. Type A・現行ルールベースAIとのベンチマークを実行する

### 評価・可視化

16. Elo レーティング計算モジュールを実装する
17. ベンチマーク結果の記録・比較レポートを実装する
18. フェーズ1のモーダルで学習済みType A/B AIの判断が表示されることを確認する

---

## 関連ファイル

| ファイル | 役割 |
|---|---|
| `AI_INVESTIGATION.md` | 現行AI仕様の全体調査結果（定数・パラメーター一覧） |
| `AI_CREATION_PHASE1_ANALYSIS_MODAL.md` | フェーズ1詳細計画（可視化・ベンチマーク閲覧） |
| `AI_CREATION_PHASE2_SUPERVISED_LEARNING.md` | フェーズ2詳細計画（モデル初期値の供給元） |
| `majiang-core/lib/game.js` | `do_sync()` の実装 |
| `majiang-core/lib/rule.js` | ルール設定（学習条件の統一に使用） |
| `tmp_clone/node_modules/@kobalab/majiang-ai/lib/player.js` | 現行ルールベースAI（ベースライン・定数の確認） |
