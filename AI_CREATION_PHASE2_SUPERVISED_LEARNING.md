# AI作成 フェーズ2: 教師あり学習パイプライン実装計画

作成日: 2026-05-27

---

## 目的

人間の天鳳牌譜を教師データとして以下の3つのモデルを学習する。
フェーズ3（強化学習）の出発点として機能させる。

| モデル | 学習タスク |
|---|---|
| 手牌類推モデル | 公開情報から他家の手牌確率を推定 |
| 行動クローン（方策モデル） | 人間の打牌・副露選択を模倣 |
| V(状態)初期モデル | 現在の局面から対局終了時の着順点を予測 |

---

## 対局ループ基盤との関係

**フェーズ2は対局ループ基盤（フェーズ3）がなくても開始できる。**
3モデルはすべて「既存の人間牌譜」を正解ラベルとして使うため、シミュレーション実行は不要。
対局ループ基盤が必要になるのはフェーズ3（強化学習のセルフプレイ）から。

---

## データ取得: 天鳳牌譜ダウンロード

### 利用規約の確認（重要）

天鳳は牌譜のAI研究利用について「明示的に禁止はしていないが積極的に許可もしていない（黙認）」の立場。
以下のルールを厳守すること。

| ルール | 内容 |
|---|---|
| ダウンロード間隔 | **20分以上** の間隔を空ける |
| 同時セッション | **1セッションのみ**（並列ダウンロード禁止） |
| 転送方式 | **圧縮ファイルのみ**（無圧縮での自動取得禁止） |
| 再配布 | 取得した牌譜を第三者に配布・公開するサービス化は禁止 |
| 競合利用 | 天鳳と競合するサービスへの応用は禁止 |
| 企業利用 | イベント協賛または support@c-egg.com への問い合わせが必要 |

robots.txt で `/sc/raw/` が `Disallow` に指定されているため、
**ダウンロードスクリプトはユーザー自身が手動実行すること**（エージェントによる自動実行は行わない）。

### ダウンロードスクリプトの設計

```js
// download_paipu.js（ユーザー手動実行用）
// 20分間隔・単一セッション・圧縮ファイル対応

const MIN_INTERVAL_MS = 20 * 60 * 1000;  // 20分

async function download_one(url, dest) {
    // 圧縮ファイルを取得
    // リダイレクト対応
    // ファイルサイズを確認して不要ダウンロードを回避
}

async function download_all(url_list) {
    for (const url of url_list) {
        await download_one(url, dest);
        await sleep(MIN_INTERVAL_MS);  // 20分待機
    }
}
```

### 推奨取得対象

- **段位**: 四段以上推奨（手が洗練されており教師データとして質が高い）
- **ルール**: 東南戦・赤あり（現行ルール設定との整合）
- **枚数目安**: 手牌類推・行動クローンには数万局分あると望ましい

---

## モデル1: 手牌類推モデル

### タスク定義

```
入力（公開情報）:
  - 自分の座席から見た各他家の捨て牌（牌の種類 × 巡数）
  - 各他家の副露内容（面子の種類と構成）
  - リーチ宣言の有無と宣言巡数
  - 残り牌数情報
  - 場風・門風・本場・残り局数

出力（確率分布）:
  - 他家ごとに「各牌を何枚持っているか」の周辺確率
    例: P(A が m1 を 1枚持っている) = 62%
        P(A が m1 を 2枚持っている) = 18%
        P(A が m1 を 0枚持っている) = 20%
  - 聴牌確率: P(A が聴牌している) = 71%
  - 当たり牌確率（聴牌時）: P(p3 が当たり牌 | A が聴牌) = 35%
```

**注意: 「全手牌パターンを全通り出力」は計算量的に不可能。**
牌ごとの周辺確率（独立した確率）として出力する。これで実用上ほぼ同等の情報が得られる。
聴牌している場合は手のパターンが大幅に絞られるため、当たり牌確率の精度が上がる。

### 正解ラベルの生成

牌譜には対局終了時（または各局終了時）の全プレイヤーの手牌が記録されている。
各手番ターン時点での他家の手牌を正解ラベルとして抽出する。

```
牌譜 → ターンごとの局面状態 → その時点での他家手牌 → 正解ラベル
```

### 特徴量設計

#### 第一原則

「プレイヤーは2〜3の連続形（順子・対子）を作るのに入らなくなったものを切っている」

この原則を起点に以下の特徴量に展開する。

#### レベル1: 手作りルールベース特徴量

```
捨て牌パターン系:
  - 各数牌スートで、どの番号帯（1-3, 4-6, 7-9）から何枚切られているか
  - 字牌を何巡目に切ったか（早巡字牌切り → 染め手・対対でない可能性）
  - 連続する番号が複数捨てられているか（例: m3 と m4 → そのスート不要の可能性）
  - 捨て牌の切り順（前半に何を切ったか vs 後半に何を切ったか）

副露パターン系:
  - 副露したスートの種類（索子のみ → 染め手の可能性）
  - ポンした牌（役牌ポン → 役牌確定、字牌→役牌狙い）
  - チーした面子（断么ルートか役なしルートか）
  - 副露タイミング（何巡目に鳴いたか）

リーチ系:
  - リーチ宣言有無
  - リーチ宣言時の巡数（早いリーチ → 好形テンパイの可能性）
  - リーチ後の捨て牌（絞り込み可能）

残り牌数系:
  - 全体の残り牌数（山残り）
  - 各牌の残り枚数（SuanPai._paishu を利用）

点数状況系:
  - 各プレイヤーの現在点数
  - 着順状況（トップ・ラス等）
  - 点差（着順ボーダーとの距離）
```

#### レベル2: ニューラルネット特徴量（後フェーズで検討）

入力を生の観測データ（one-hot encoding等）のまま与え、特徴量をモデルが自動で学習する。
第一原則的なパターンはモデルが自分で発見する。解釈性は下がるが表現力は上がる。

### 評価指標

```
- 各牌の保有枚数予測の精度（交差エントロピー）
- 聴牌確率の予測精度（AUC-ROC）
- 当たり牌予測の精度（Top-K accuracy）
- 聴牌条件での手パターン絞り込みの精度（聴牌時限定）
```

**評価には学習に使っていない牌譜（held-out set）を使うこと。**

---

## モデル2: 行動クローン（方策モデル）

### タスク定義

```
入力:
  - 自分の手牌（Shoupai）
  - 手牌類推モデルの出力（他家の手牌確率）
  - SuanPai の状態（残り牌数、危険度情報）
  - 点数状況・場況

出力:
  - 合法手の確率分布
    打牌: P(m1) = 0.05, P(m2) = 0.32, ..., P(リーチm3) = 0.28, ...
    副露: P(パス) = 0.70, P(ポンm1++=) = 0.20, P(チーm123-) = 0.10
    和了: P(和了) = 0.95, P(パス) = 0.05
```

### 正解ラベルの生成

牌譜の各手番で「人間が実際に選んだアクション」を正解ラベルとする（one-hot）。

```
牌譜 → ターンごとの局面状態 → その時点での合法手一覧 → 実際に選んだアクション → 正解ラベル
```

### 留意点

- 行動クローンは「人間の平均的な打牌を模倣する」モデルであり、強くなる上限は人間の平均になる
- フェーズ3の強化学習の初期化に使い、そこから上を目指す
- 手牌類推モデルの出力を特徴量として組み込むことで、「見えている情報に基づいた行動」の模倣になる

---

## モデル3: V(状態)初期モデル（価値関数の初期化）

### タスク定義

```
入力:
  - 現在の局面状態（自手牌、残り牌数、点数状況、場況等）

出力:
  - この局面から対局終了までの着順点の期待値（スカラー）
    例: +8.5（平均的に2位相当の着順点が期待できる）
```

### 正解ラベルの生成

```
牌譜 → 各ターンの局面状態 → 対局の最終着順点 → 正解ラベル
```

対局の最終着順点（`paipu.point[id]`）を各ターンの正解として使う（単純化）。
これにより「この局面が最終的にどれくらいの着順点につながったか」を学習する。

### フェーズ3への役割

フェーズ3のActor-Criticにおいて、このモデルがCritic（価値関数）の初期値になる。
初期値がある程度良ければ、フェーズ3の学習が速く安定する。

### 限界と受け入れ方

このモデルが学習するのは「人間が打った場合の着順点期待値」であり、「最適に打った場合の期待値」ではない。
フェーズ3のセルフプレイで人間を超える部分を獲得することになる。

---

## 着順期待値への教師あり学習の適切さについて

「着順期待値への影響」をモデル化するのに教師あり学習は適切か？という疑問について。

**部分的にYes、ただし限界を理解した上で使う:**

```
OK な点:
  - 人間牌譜に最終着順のラベルを付ければ回帰問題として学習できる
  - 「この点数状況・局面からこうなった」という統計的パターンは取れる
  - フェーズ3の開始点として十分

限界:
  - 人間牌譜は人間の実力に依存するため、
    「人間が最善手を打った場合の期待値」しか学習できない
  - 最適戦略による期待値はRL（フェーズ3）で初めて獲得できる
```

---

## 実験管理の仕組み

フェーズ2は何度も特徴量を変えて再学習する。モデルバージョン管理が重要。

### 最低限必要なもの（シンプル版）

```
models/
  hand_inference/
    v1/
      model.pt（またはmodel.pkl等）
      config.json    ← 特徴量セット・ハイパーパラメーター
      eval_result.json  ← AUC・accuracy等の評価結果
    v2/
      ...
  behavior_clone/
    v1/ ...
  value_function/
    v1/ ...
```

### フェーズ1 UIとの連携

- どのモデルバージョンが使われているかを分析モーダルに表示する
- どの特徴量が使われているかを `config.json` から読み込んで表示する
- モデル間の切り替えをUIから行えるようにする

---

## 実装ステップ

### データパイプライン

1. 天鳳牌譜ダウンロードスクリプトを作成する（20分間隔・単一セッション・圧縮対応）
2. 牌譜XMLをパースして局面状態の時系列に変換するモジュールを作成する
3. 各モデル用の特徴量ベクトルと正解ラベルを生成するモジュールを作成する
4. 学習用・検証用・テスト用にデータを分割する

### モデル学習

5. 手牌類推モデルを実装・学習する（特徴量レベル1から始める）
6. 行動クローンモデルを実装・学習する
7. V(状態)初期モデルを実装・学習する
8. 各モデルを評価指標で評価し、結果を記録する

### フェーズ1 UIとの統合

9. 学習済みモデルの推論インターフェースを統一する（`HandInferenceModel`, `PolicyModel`）
10. フェーズ1のUIスロットに学習済みモデルの出力を差し込む
11. 分析モーダルで「手牌推定」「聴牌確率」「当たり牌確率」が表示されることを確認する

### 反復改善

12. 分析モーダルで推定結果の精度を目視確認し、外れている局面を分析する
13. 特徴量を追加・修正して再学習する（→ステップ5〜8に戻る）

---

## フェーズ間の依存関係

```
フェーズ1（分析モーダル）
  ↓  UIスロットが用意される
フェーズ2（教師あり学習）
  ↓  学習済みモデルをスロットに差し込む
フェーズ2+1（UIで推定精度を目視確認・改善）
  ↓  モデルが十分な精度になる
フェーズ3（強化学習）
  ← 手牌類推モデルを特徴量として使用
  ← 行動クローンで方策を初期化
  ← V(状態)モデルでCriticを初期化
```

---

## 関連ファイル

| ファイル | 役割 |
|---|---|
| `AI_INVESTIGATION.md` | 現行AI仕様の全体調査結果 |
| `AI_CREATION_PHASE1_ANALYSIS_MODAL.md` | フェーズ1詳細計画（UIスロット設計） |
| `AI_CREATION_PHASE3_REINFORCEMENT_LEARNING.md` | フェーズ3詳細計画（V関数の継承先） |
